// TECHSURGE 2K26 — KALACHAKRA
// ---------------------------------------------------------------------------
// The countdown begins the moment someone presses Start on screen — there's
// no fixed clock time to configure. To change how long it runs, edit this:

/** How long the countdown runs for, in hours, once launched. */
export const DURATION_HOURS = 28;

// ---------------------------------------------------------------------------

export const EVENT_NAME = 'TECHSURGE 2K26';
export const EVENT_SUBTITLE = 'KALACHAKRA';
export const EVENT_TAG = `${DURATION_HOURS} HOUR HACKATHON`;

export const STATUS_READY = 'READY TO START';
export const STATUS_LIVE = 'HACKATHON IN PROGRESS';
/** Shown briefly in place of EVENT_TAG right after launch, then reverts. */
export const TAGLINE_TRANSIENT = `${DURATION_HOURS} HOURS. ONE CHALLENGE.`;
